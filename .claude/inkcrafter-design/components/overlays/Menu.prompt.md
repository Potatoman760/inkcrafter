Floating menus, and the shape of the ink right-click menu.

```jsx
<Menu label="in knot the_cove" style={{position:'fixed',left:x,top:y}}>
  <MenuItem icon="git-branch" keys="Ctrl+Alt+C">Add a choice…</MenuItem>
  <MenuItem icon="package">Give an item…</MenuItem>
  <MenuSeparator />
  <MenuItem icon="trash-2" danger>Delete this line</MenuItem>
</Menu>
```

Group by what the item edits, separators between groups, destructive last and alone.
