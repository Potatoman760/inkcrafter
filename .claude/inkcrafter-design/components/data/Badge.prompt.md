Monospace count or state marker inside a row or card.

```jsx
<Badge variant="accent">main</Badge>
<Badge variant="zero">0</Badge>
```

Zero counts render as `zero` (faint, no fill) rather than being hidden — an author needs to see that nothing mentions an entry.
