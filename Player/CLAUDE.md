# CLAUDE.md

The guidance for this repository lives in **[Agents.md](Agents.md)** — commands,
architecture, the critical gotchas, and where the interchange spec comes from.

This file exists only because Claude Code looks for this filename. Keeping one
document rather than two means the two can never drift apart and disagree about
what the code does. Edit Agents.md; leave this pointer alone.

The line below imports it, so the whole thing is in context either way:

@Agents.md
