The single row primitive for every list in the app.

```jsx
<ListRow icon="file-text" name="chapter3.ink" mono selected
  trail={<Badge variant="accent">main</Badge>} />
```

One icon vocabulary per list. Put counts and states in `trail` as `Badge`; put paths in `meta`.
