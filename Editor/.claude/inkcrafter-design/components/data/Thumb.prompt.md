Asset preview for the media library, cast sprites and map backgrounds.

```jsx
<Thumb size="wide" src={url} alt="bg/cove" />
<Thumb size="sm" missing />
<Placeholder label="background 1920×1080" style={{aspectRatio:'16/9'}} />
```

`Placeholder` is the correct answer to art you do not have — never draw a stand-in illustration.
