Compact exclusive switch for 2–4 short options — plan Grid/Matrix, word-limit presets.

```jsx
<Segmented value={mode} onChange={setMode}
  options={[{value:'grid',label:'Grid'},{value:'matrix',label:'Matrix'}]} />
```

Not a tab strip: it switches a control's setting, not the pane's content.
