Icon-only square control for pane headers, toolbars and row affordances.

```jsx
<IconButton icon="panel-right" label="Toggle the dock" active />
```

Use only where the icon is unambiguous (close, expand, reveal, toggle). Anything a user has to guess at gets a `Button` with words.
