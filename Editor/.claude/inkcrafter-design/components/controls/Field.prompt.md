Wraps a control with its label and hint.

```jsx
<Field label="Entry point" hint="Compilation starts from this file.">
  <Select value={main}>…</Select>
</Field>
```

Stacked by default; `inline` in master–detail panes where a column of labels reads faster.
