Lucide glyph tinted with currentColor — the only icon primitive in the system.

```jsx
<Icon name="file-text" size={14} />
```

Names are Lucide kebab-case. The house set: file-text, folder, book-open, map, users, package, image, sparkles, play, git-branch, panel-right, search, settings, plus, x, chevron-right, circle-alert, triangle-alert, check, rotate-ccw. Never mix in another icon family, and never fall back to emoji.
